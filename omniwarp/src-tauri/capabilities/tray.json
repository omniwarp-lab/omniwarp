{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-window",
  "description": "Capability for the custom tray menu window",
  "platforms": ["windows"],
  "windows": ["tray"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "store:default"
  ]
}
